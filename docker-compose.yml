version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: orders_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: orders
    ports:
      - "5432:5432"
